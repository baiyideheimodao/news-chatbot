name: flutter_desktop_chat
description: A desktop Flutter demo with a floating window and chat UI.
publish_to: none
version: 0.1.0
environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.3.0
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/redboy.png
